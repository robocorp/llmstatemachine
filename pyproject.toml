[tool.poetry]
name = "llmstatemachine"
version = "0.1.0"
description = ""
authors = ["Mikko Korpela <mikko@robocorp.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "~3.8"
openai = "^1.3.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
